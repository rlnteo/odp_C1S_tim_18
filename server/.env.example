# ── Master — uvek prima pisanje (INSERT/UPDATE/DELETE) ────────
DB_MASTER_HOST=localhost
DB_MASTER_PORT=3306
DB_MASTER_USER=root
DB_MASTER_PASSWORD=
DB_MASTER_NAME=dbdocker

# ── Slave 1 — čitanje Round-Robin ─────────────────────────────
DB_SLAVE1_HOST=localhost
DB_SLAVE1_PORT=3307
DB_SLAVE1_USER=root
DB_SLAVE1_PASSWORD=
DB_SLAVE1_NAME=dbdocker

# ── Slave 2 — čitanje Round-Robin ─────────────────────────────
DB_SLAVE2_HOST=localhost
DB_SLAVE2_PORT=3308
DB_SLAVE2_USER=root
DB_SLAVE2_PASSWORD=
DB_SLAVE2_NAME=dbdocker

# ── Express ───────────────────────────────────────────────────
PORT=4000
CLIENT_URL=http://localhost:5173

# ── JWT ───────────────────────────────────────────────────────
JWT_SECRET=

# ── Bcrypt ────────────────────────────────────────────────────
SALT_ROUNDS=10